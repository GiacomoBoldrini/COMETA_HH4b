# COMETA_HH4b

Simple script to setup a virtual environment with needed python packages to analyze HH4b root files and a simple python script to read them and plot distributions 

```
source setup_env.sh
```

Properly copy the data folders (2b_control_region_only, ...) in this folder and then 

```
python plot_regions.py